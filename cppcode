// C++ code
//
void setup()
{
    pinMode(A0, INPUT);    pinMode(LED_BUILTIN, OUTPUT);    pinMode(9, OUTPUT);
    pinMode(7, OUTPUT);
}

void loop()
{
    if ((-40 + 0.488155 * (analogRead(A0) - 20)) < 24 {
       digitalWrite(LED_BUILTIN, LOW);       tone(9, 523, 1000);       digitalWrite(7, HIGH);       delay(1000);
       tone(9, 523, 1000);       digitalWrite(7, LOW);
       delay(100);    }  else {
       digitalWrite(LED_BUILTIN,HIGH);
    }
}
